node_modules/
dist/
*.tsbuildinfo
.DS_Store
Thumbs.db

# Publish staging (assembled by scripts/pack.mjs; the tgz is a build artifact)
staging/
agent2llm-*.tgz

# Local Agent2LLM state (secrets, pairing, sessions) — never in the repo.
.agent2llm/
*.log
.env
.env.*
!.env.example

# Throwaway probe scripts written while debugging an adapter by hand.
scratch-*.mjs
